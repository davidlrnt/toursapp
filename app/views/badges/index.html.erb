<% Badge.all.each do |badge| %>
	<%if @user.badges.include?(badge) %>
		You achieved : <%= badge.name %><br><br>
	<% else %>
		 to be <%= badge.name %><br>
		require:<%= badge.description %><br>
	<% end %>
<% end %>
